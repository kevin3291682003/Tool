~case CMD_DIAL_CGDCONT_S:

1 USB
2 UART

int do_curl_full(
    CURL_METHOD method,            // 0=GET,1=POST,2=UPLOAD
    const char* url,
    const char* form,              // "a=1&b=2"，可 NULL
    const char* uploadPath,        // 檔案路徑，僅 UPLOAD 有用
    const char* downloadPath,      // 結果存檔名，可 NULL
    const char* token,             // Bearer token，可 NULL
    int use_form_f,                // 0:-d application/x-www-form-urlencoded；1:-F multipart
    int print_progress,             // 1:有進度條/stdout；0:silent
    int dump,                       // 1:有Dump
) {
